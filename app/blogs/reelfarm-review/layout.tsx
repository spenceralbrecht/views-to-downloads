import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'ReelFarm Review: Automated TikTok Video Creation vs. ViewsToDownloads',
  description: 'An in-depth review of ReelFarm\'s AI video creation features, pricing, and comparison with alternatives like ViewsToDownloads.com for TikTok marketing.',
};

export default function ReelfarmReviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 